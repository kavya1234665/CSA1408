%{
#include <stdio.h>
#include <string.h>
%}

%%
abc     { printf("ABC"); }
.       { printf("%s", yytext); }
\n      { printf("\n"); }
%%

int main()
{
    printf("Enter text:\n");
    yylex();
    return 0;
}

int yywrap()
{
    return 1;
}
