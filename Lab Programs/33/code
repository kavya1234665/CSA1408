%{
#include <stdio.h>
%}

%%
https?:\/\/(www\.)?[a-zA-Z0-9.-]+\.[a-zA-Z]{2,}([\/a-zA-Z0-9._?=&%-]*)? {
    printf("Valid URL: %s\n", yytext);
}

.* {
    printf("Invalid URL: %s\n", yytext);
}
%%

int yywrap()
{
    return 1;
}

int main()
{
    printf("Enter URL: ");
    yylex();
    return 0;
}
