%{
#include <stdio.h>
%}

%%
[0-9]+[ \t]*\+[ \t]*[0-9]+ {
    printf("Addition operation: %s\n", yytext);
}

[0-9]+[ \t]*-[ \t]*[0-9]+ {
    printf("Subtraction operation: %s\n", yytext);
}

[0-9]+[ \t]*\*[ \t]*[0-9]+ {
    printf("Multiplication operation: %s\n", yytext);
}

[0-9]+[ \t]*\/[ \t]*[0-9]+ {
    printf("Division operation: %s\n", yytext);
}

. ;
%%

int yywrap()
{
    return 1;
}

int main()
{
    printf("Enter mathematical expression:\n");
    yylex();
    return 0;
}
