%{
#include <stdio.h>
%}

%%
[0-9]+              { printf("%s is a constant\n", yytext); }
[0-9]+\.[0-9]+      { printf("%s is a constant\n", yytext); }
[a-zA-Z]+           { }
[ \t\n]+            { }
.                   { }
%%

int main()
{
    printf("Enter the input:\n");
    yylex();
    return 0;
}

int yywrap()
{
    return 1;
}
